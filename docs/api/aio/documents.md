# Async Documents

`AsyncDocumentNamespace` for the `documents.document` model (Odoo Enterprise), accessed as `client.documents` on `AsyncOdooClient`.

::: vodoo.aio.documents
    options:
      show_source: true
      members_order: source
