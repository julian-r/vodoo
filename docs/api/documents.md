# Documents

`DocumentNamespace` for the `documents.document` model (Odoo Enterprise), accessed as `client.documents`.

::: vodoo.documents
    options:
      show_source: true
      members_order: source
